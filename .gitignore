# Secrets — never commit real credentials
.env

# Python — generated at runtime/test time, safe to regenerate
__pycache__
.pytest_cache
.venv
venv

# Node — generated by landing/ (VitePress site), safe to regenerate
node_modules

# Editor-specific settings
.vscode

# Local scratch (pre-existing rule, kept as-is)
test

# Claude Code — local tooling config and internal workflow artifacts
# (skills' issue-tracking board, project instructions); not meant to
# ship as part of the bot's codebase
.claude
CLAUDE.md
issues
