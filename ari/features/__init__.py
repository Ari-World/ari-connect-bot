"""Self-contained product features. `global_chat` is the only one today
and is meant to be the template for the next one — copy its internal
shape (models/state/repository/services/cogs/ui/presenters) rather than
inventing a new pattern. See CLAUDE.md's "Adding a new feature".
"""
